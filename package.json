{
  "devDependencies": {
    "@webdiscus/pug-loader": "^2.10",
    "@webpack-cli/generators": "^3.0.7",
    "copy-webpack-plugin": "^12.0.2",
    "css-loader": "^6.9.1",
    "enhanced-resolve": "^5.7",
    "prettier": "^3.2.4",
    "pug": "^3.0.2",
    "pug-plugin": "^4.9.9",
    "sass": "^1.70.0",
    "sass-loader": "^14.0.0",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4",
    "webpack-dev-server": "^4.15.1"
  },
  "version": "1.0.0",
  "description": "High Tech Surveillance website",
  "name": "hts-website",
  "scripts": {
    "prebuild": "rm -f dist/*",
    "build": "webpack --mode=production --node-env=production",
    "build:dev": "webpack --mode=development",
    "build:prod": "webpack --mode=production --node-env=production",
    "dev": "webpack serve --mode development --env development --live-reload",
    "deploy": "GIT_DIR=./dist/.git GIT_WORK_TREE=./dist/ sh -c \"git add -A && git commit -m 'new: user: New release deployed' && git push\"",
    "watch": "webpack --watch"
  }
}
